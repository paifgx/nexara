#!/bin/bash

# Coverage configuration
# You can adjust these values as needed during development

# Coverage threshold percentage (0-100)
export COVERAGE_THRESHOLD=80

# Set to "true" to skip coverage check temporarily during development
# IMPORTANT: Set back to "false" before merging to main branch
export SKIP_COVERAGE_CHECK="false"
